services:
  formfit-ai:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - ENVIRONMENT=production
      - API_PORT=8000
    volumes:
      - formfit-data:/app/data
    restart: unless-stopped

volumes:
  formfit-data:
